import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    inputWrapper: {
      flexDirection: 'row',
      alignItems: 'center',
      borderWidth: 1,
      borderColor: '#000',
      borderRadius: 5,
      height: 50,
    },
    inputIcon: {
      marginLeft: 10,
    },
    input: {
      flex: 1,
      fontSize: 16,
      marginLeft: 10,
    },
  });